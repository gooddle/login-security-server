# 2026-06-20

## 프로젝트 초기 세팅
- FastAPI + Python 프로젝트 구조 생성
- SQLite + SQLAlchemy 데이터베이스 설정
- pytest TDD 하네스 구축 (pre-commit hook, GitHub Actions CI)
- Claude Code 하네스 (CLAUDE.md, hooks, commands)

## feat: 로그인 API (POST /auth/login)
- IP 기반 새 로그인 감지
- 새 IP 접근 시 이메일 알림 발송 (Resend)
- 기존 IP는 알림 없이 통과
- 테스트: 4개 (새 IP, 기존 IP, 인증 실패, 이메일 횟수)

## refactor: REST → GraphQL 완전 교체
- Strawberry GraphQL 도입
- `/graphql` 단일 엔드포인트로 통합
- Mutation: `signup`, `login`
- Query: `health`

## feat: 회원가입 API (POST /auth/signup)
- 이메일 형식 검증
- 비밀번호 최소 8자 검증
- 중복 이메일 409 응답
- 테스트: 4개 (성공, 중복, 이메일 형식, 비밀번호 길이)

## feat: 5회 로그인 실패 시 계정 잠금
- User 모델에 `failed_attempts` (int, default 0), `locked_until` (datetime, nullable) 추가
- `auth_service`: `is_account_locked`, `record_failed_attempt`, `reset_failed_attempts` 함수 추가
- 5회 실패 → 30분 잠금, 잠금 만료 시 자동 해제
- 잠금 중 남은 시간(N분 N초) 에러 메시지 반환
- 테스트: 5개 추가 (카운터 증가, 잠금 발생, 남은 시간, 자동 해제, 성공 시 초기화)
